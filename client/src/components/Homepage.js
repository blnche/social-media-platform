export default function Homepage() {
    return (
        <h2>Hello Homepage</h2>
        // If not connected then background image with link to authenticate
        // Else show feed
        // Feed : list of posts 
        // Post actions : view user profile, view post location, follow user, like post, open post comments 
        // open post comments : add comment, remove user connected comment, like comment, signal comment, view user profile
    );
}