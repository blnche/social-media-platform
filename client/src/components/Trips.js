export default function Trips() {
    return (
        <h2>Hello Trips</h2>
        // Filter : dates, location, budget, locationType[sea, mountain, desert, forest], groupType[mixed, grils only, boys only], organisationType[planned, notPlanned, semi-planned]

        // list of available Trips
        // Trip : user profile offering the trip, location, actions[message user, follow trip], badges corresponding to trips characteristics, numbers of participants, dates, stopsor highlights ??, view know more
        // view know more : actions[close, join, message, follow], same infos from list, Trip title if exist

    );
}